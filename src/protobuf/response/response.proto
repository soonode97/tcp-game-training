syntax = "proto3";

package response;

// 클라이언트에서 보내는 패킷에 대한 내용
message Response {
    uint32 handlerId = 1;
    uint32 responseCode = 2;
    int64 timestamp = 3;
    bytes data = 4;
    uint32 sequence = 5;
}