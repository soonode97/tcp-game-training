syntax = "proto3";

package game;

message CreateGamePayload {
    int64 timestamp = 1;    // 게임 생성 시간
}

message JoinGamePayload {
    string gameId = 1;      // 어떤 게임 세션인지 확인
    int64 timestamp = 2;    // 게임에 참가한 시간
}

message Start {
    string gameId = 1;
    int64 timestamp =2;
}